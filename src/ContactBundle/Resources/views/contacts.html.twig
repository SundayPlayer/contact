{% extends "ContactBundle:Default:index.html.twig" %}
{% block body %}
    {{ parent() }}
    {% if contacts|length > 0 %}
        <ul>
            {% for contact in contacts %}
                <li> <a href="contact/{{contact.id}}">{{ contact.firstName }}</a>({{ contact.lastName }})</li>
                {% endfor %}
        </ul>
    {% else %}
        <p>No contacts</p>
    {% endif %}
    <a href="{{ path('addContact') }}">Add contact</a>
{% endblock %}