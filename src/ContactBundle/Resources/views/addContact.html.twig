{% extends "ContactBundle:Default:index.html.twig" %}
{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Enregistrer le contact</button>
    {{ form_end(form) }}
{% endblock %}